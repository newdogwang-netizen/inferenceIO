# schemas changelog

规则：同一版本内只允许新增可选字段；删除字段或改变语义必须新建 `v2` 文件。

## v1 (2026-09-15)

- `event.v1`：事件 envelope。见 platform/03 §1～2。
- `batch.v1`：批次头。见 platform/03 §3。
- `capabilities.v1`：采集端能力清单。见 platform/09 §2。
