# Support and verification matrix

Status meanings:

- `verified`: covered by an automated regression in this repository.
- `experimental`: implemented, but its environment/version matrix is not yet broad enough for a completeness claim.
- `unsupported`: deliberately not implemented in this release.
- `unknown`: not exercised against the required real target here.

The authoritative machine-readable companion is [`support-matrix.v1.json`](support-matrix.v1.json), also embedded in `iorec support --json`. It is fail-closed: unlisted combinations default to `unknown`, verified cells cannot use wildcard Agent/runtime/TLS dimensions, and their source/artifact evidence is checked automatically (artifacts are SHA-256 pinned). This prose table gives the broader operator-facing surface summary without upgrading any exact matrix cell.

September 18 development scope: the [M1 real-capture report](benchmarks/2026-09-18-websocket-call-projection-linux-x86_64.json)
verifies 19 individual Responses WebSocket calls, exact message ownership and
repeatable reprocessing for its named Codex recording. M2's evidence/workflow
changes do not broaden the agent/provider/version cells below. Worker heartbeats
indicate process/database liveness, not successful job processing. A Harbor
score attached by an operator is labeled `operator_reported_not_recomputed`,
not promoted to platform-owned transport proof. Historical five-hour gates do
not qualify current candidate binaries or images; M3 will need a new gate.
The pinned [native Harbor installation checks](benchmarks/2026-09-18-native-harbor-install-linux-x86_64.json)
and [Hermes installed-runtime check](benchmarks/2026-09-18-hermes-runtime-and-portable-calibration-linux-x86_64.json)
cover container setup/version compatibility only; they do not upgrade any real
agent/provider transport cell below.

| Surface | Status | Scope |
|---|---|---|
| Linux x86-64 | verified | Build, unit tests, subprocess runner, signal forwarding, `/proc` tracking |
| Process network-gap accounting | verified | Socket-table/FD permission and parse failures become deduplicated evidence and nonzero `unparsed_connections`; surviving descendants/open connections make the run incomplete; raw proc rows are not stored |
| Process exec and long-run bounds | verified | Same-PID executable/name changes are recorded; aggregate connection/gap scans and cross-run reported-gap history have explicit fail-closed ceilings |
| Launch-time model-bypass classification | verified | Up to 64 resolved upstream socket addresses are compared with target-process sockets; direct matches are counted separately and fail transport verification, while DNS churn/unmatched endpoints remain unknown |
| Auxiliary egress classification | experimental | Repeatable bounded `auth`/`telemetry`/`update`/`other` HOST:PORT rules resolve before target release, emit an auditable exact-socket snapshot, reject model/cross-class collisions, and are recomputed locally and by the platform. Parser, conflict, fail-closed inspection, CLI, and live-socket regressions pass; SNI/hostname confirmation and broad DNS/CDN matrices remain unqualified |
| Executable TLS fingerprinting | experimental | Full-file hash pass also finds bounded marker classes for BoringSSL, rustls, Go `crypto/tls`, OpenSSL, GnuTLS, and NSS; marker presence remains unknown coverage, not proof of an active or covered TLS path |
| Passive Agent version discovery | experimental | Constrained native paths, exact supported npm package names, and unambiguous Hermes venv distribution metadata; Codex 0.154.0, Claude 2.1.273, Gemini CLI 0.60.0, and Hermes 0.19.0 are detected in the pinned real-CLI qualification without executing a discovery command |
| Preflight probe planner | verified | Versioned manifest plan and read-only `iorec plan` CLI combine selected modes, passive runtime/TLS markers, intended protocols, current pcap/eBPF prerequisites, and AgentSight/eCapture availability without executing the target. Candidate/selection consistency is validated; unavailable or unbridged tools remain explicit and static markers never become coverage proof. Active post-launch surfaces remain in separate coverage fields. |
| Durable event/blob writer | verified | Contiguous sequence, per-batch `sync_data` before acknowledgement, prefix-safe capacity failure, 24 MiB record parity, 4 GiB default physical log budget, atomic 2 GiB/250,000-entry blob reservations, recovery, exclusive writer lock; batching adds no timer delay |
| Rust 1.89 MSRV | verified | `cargo check --all-targets --all-features` |
| HTTP/1.1 reverse proxy | verified | Streaming request/response tee, status/errors, no redirect following; 429/500 retries and mid-stream reset prefixes are exercised end to end |
| SSE | verified | Boundary splitting, multi-line events, cancellation, unterminated tails, response-limit enforcement across secondary event blobs, 100 concurrent streams |
| WebSocket | verified | Both directions, opcode, close, reconnect IDs; wire fragmentation is not preserved |
| HTTP/2 upstream client | experimental | 100 concurrent h2c streams pass without cross-talk under explicit prior-knowledge mode; a local certificate-validated TLS/ALPN cell reconstructs two concurrent padded HTTP/2 streams without cross-talk. Provider/version matrices remain unverified |
| OpenAI-compatible APIs | verified | Controlled fake server and real child-process requests |
| Anthropic-compatible APIs | experimental | Generic message/SSE transport works; credential modes and signed cloud endpoints need matrix testing |
| Codex adapter | verified | Exact Codex 0.154.0 executable completed a real Responses SSE call through the controlled loopback provider; session/transport correlation and the independent task-egress audit passed. Public-provider and other version cells remain unknown. |
| Claude Code adapter | verified | Exact native Claude Code 2.1.273 completed concurrent main/internal Anthropic SSE calls; lifecycle/session correlation and independent task-egress audit passed. OAuth, Bedrock, Vertex, other versions, and a native model hook remain outside this cell. |
| Hermes adapter | verified | Exact Hermes 0.19.0 completed a real Chat Completions call; the observer/transport correlation and independent task-egress audit passed while six non-model requests were classified separately. Public/fallback providers and other versions remain outside this cell. |
| Gemini CLI adapter | verified | Exact Gemini CLI 0.60.0 completed a real generate-content SSE call; model/session hooks and independent task-egress evidence correlated without ambiguity. Public providers, other versions, and non-text hook completeness remain outside this cell. |
| Python OpenAI SDK/runtime injection | experimental | Private `sitecustomize` bootstrap; sync/async Chat Completions and Responses; sync/async httpx and requests send/response iterators; model and HTTP stream completion/error/cancellation; fork reinitialization; bounded queue/stream/URL/header handling; credential redaction and loss gating. Synthetic regression plus installed Python 3.11/OpenAI SDK 2.24.0 and real httpx no-network smokes pass, but no broad SDK/runtime matrix yet. |
| Node.js runtime injection | experimental | Private CommonJS preload plus ESM loader; OpenAI Chat Completions/Responses, global fetch and CommonJS undici entry points, stream completion/error/cancellation, worker/fork inheritance, bounded queue/clone/sender behavior, credential redaction, and loss gating are covered. Installed OpenAI JS 7.15.0 CJS/ESM and ESM streaming no-network smokes pass; direct ESM undici and a broad Node/SDK matrix remain unverified, while Bun is rejected. |
| TLS key-log ingestion | experimental | NSS-style FIFO validation, deduplication, encryption, explicit export, shutdown priority under continuous input, bounded oversized-input rejection, and drain-only non-interference after storage failure verified; the recorder's certificate-validating rustls upstream client and transparent TLS server write to the same encrypted pipeline |
| Node `--tls-keylog` injection | experimental | Direct Node and Node shebang detection, `NODE_OPTIONS` preservation/conflict rejection, FIFO ingestion, and export verified |
| Filtered classic pcap ingestion | experimental | Header/chunk/drop/lifecycle, immediate capture, continuous bounded-tail stderr drain, sanitized-helper environment, and bounded numeric upstream-address filters verified; live capture passed local TLS HTTP/1.1 and HTTP/2 cells. The default launch-time address snapshot is neither a task-egress boundary nor an egress firewall |
| Rootless proxy-only task network | verified | Linux x86-64 user/network namespace launch preserves UID/GID while dropping all target capabilities, disables DNS/IPv6, installs and re-verifies an exact nftables allowlist, captures all namespace IP packets without host `CAP_NET_RAW`, and measures only the target window. Automated positive and direct-IP bypass cases pass repeatedly; other OS/architecture/container/helper-version cells remain unqualified |
| Rootless transparent task network | experimental | Original non-loopback IPv4 model sockets are pinned through a private read-only `/etc/hosts` mount and exact nftables DNAT; loopback and other unusable endpoints fail before launch, and target privileges are dropped before release. Per-run in-memory TLS keys, temporary public CA cleanup, HTTP/1.1, HTTP/2 ALPN, WSS, 32 stalled-handshake concurrency, encrypted pcap/key-log decryption, and exact payload audit pass on one Linux x86-64 cell. Custom DNS/trust stores, certificate pinning, QUIC, containers, arm64, macOS, providers, and auxiliary egress remain unqualified |
| Offline pcap/TLS transport audit | experimental | Trusted TShark 4.4+, bounded private decryption, HTTP/1.1 and multiplexed/padded HTTP/2 reconstruction, and multiset exact-body comparison are implemented. Automated rootless cleartext/transparent cells and exact Gemini/Codex/Claude/Hermes controlled-provider cells return complete; a blocked bypass retains payload agreement but returns incomplete. Broad public-provider/protocol matrices remain unverified. |
| Versioned privileged-helper protocol | verified | Protocol v2 binds readiness to the exact target PID, canonical executable SHA-256, and PID-tree/cgroup filter; canonical helper trust checks, pre-exec stop/ready barrier, empty environment, bounded encrypted JSONL ingestion, payload digest validation, drop/final/count gates, shutdown, and target exit-code preservation are covered. No third-party bridge is qualified by this row. |
| eCapture TLS bridge | experimental | Digest-pinned eCapture v2.6.0 synchronous text/hex bridge with cgroup/PID binding, bounded membership history, drop conversion, delayed source drain, encrypted evidence, and private probe export. Exact bytes, descendant capture, outside-cgroup isolation, four concurrent connections, and 1 MiB aggregate long payload pass on Debian 13 x86-64/kernel 6.12/curl OpenSSL 3.5.7/HTTP/1.1 only. This host required broad capabilities including `CAP_SYS_ADMIN`; all other matrix cells remain unknown. |
| eCapture GoTLS exact cells | verified | Digest-pinned eCapture v2.6.0 plus bridge 1.1.0 passes Go 1.24.13 and 1.25.13 on Debian 13 x86-64/kernel 6.12/HTTP/1.1. Each encrypted run reassembles four concurrent 64 KiB bodies in both directions with one ID per connection, zero drops, exact payload hashes, and complete helper final; two concurrent requests from the same binary outside the task cgroup are excluded. Other Go releases, named Agents/providers, HTTP/2, OS, architecture, and container cells default to unknown. |
| AgentSight/eBPF process bridge | experimental | Digest-pinned AgentSight v1.0.25 `debug process` bridge with cgroup binding, bounded process/file/network/coordination/memory JSON evidence, loss conversion, encrypted export, and one Debian 13 x86-64/kernel 6.12 live cell. Functional and concurrent outside-cgroup-isolation runs pass integrity, but both finals correctly remain incomplete: upstream ring-buffer reservation loss is uncounted, file rate limiting fired, and the raw end anchor was absent. TLS capture, other platforms, and completeness remain unqualified. |
| OpenInference JSONL export | verified | AGENT/LLM spans, stable IDs, retry linkage, payload and token attributes; bounded per-attempt payload references and evidence-sequence arrays |
| OTLP/HTTP JSON export | verified | Standard `ExportTraceServiceRequest` shape, protobuf JSON mapping, `/v1/traces` metadata, shared OpenInference attributes; offline file only |
| Replay bundle export | verified | Exact captured HTTP request/response bytes, hashes, sanitized metadata, state limitations, bounded per-attempt evidence, and no credentials; model generation is not made deterministic |
| Long-lived logical-task splitting | verified | `agent-task-or-session-v1` keeps one immutable run/global sequence while explicit `task_id` splits interleaved work and `session_id` provides the conservative fallback. An encrypted zstd Hermes gateway regression interleaves 256 hooks across two tasks; bounded local indexes, correlation/exports, multi-segment platform rebuild/query, idempotent reprocessing, and Web rendering are covered. |
| Timeline pagination | verified | Complete-log validation with bounded 10,000-entry default pages, a 100,000-entry hard limit, and sequence cursors; no silent result truncation |
| State-analysis bounds | verified | Per-category 100,000-item bounds preserve run finalization and turn omitted observations into an explicit unresolved completeness gate |
| Offline analysis bounds | verified | Correlation/inspection/OpenInference/replay indexes and blob inventories have explicit cardinality limits; exact operations return `analysis_limit` rather than silently truncating |
| Multimodal dependency gate | verified | Inline data is distinguished from unsnapshotted image URLs, file IDs, and file URIs without copying reference values into summaries |
| Encrypted manifest authentication | verified | AEAD canonical-content authentication, tamper/wrong-key tests, and legacy migration |
| Class-keyed evidence retention | verified | New encrypted runs wrap random `body`/`pcap`/`tls_secrets` keys under the run key; independent TTL and explicit/remote class erasure authenticate intent/completion, resume after crashes, require upload ACK/seal and writer exclusion, and leave other classes readable. Physical-media erasure is explicitly not claimed |
| Whole-run age pruning | verified | Preview default, finalized-run validation, authenticated encrypted deletion, quarantine rename, and audit records |
| Hook trust isolation | verified | Target-controlled hook sources are namespaced, recorder sources are reserved, and hook event names cannot spoof transport/process/correlation/TLS/pcap coverage or exports |
| Local audit hash chain | verified | Ordered append, concurrent locking, full-chain verification before every append, tamper/truncation/tail detection; no signature or external anchor |
| Performance/soak harness | verified | Loopback-only direct/recorded comparison, encrypted full-body workload, atomic reports, count/drop/integrity post-validation; no noisy CI thresholds |
| HTTP/2 TLS offline decode | experimental | One local TLS/ALPN qualification cell reconstructs concurrent padded streams and exact DATA lengths; provider/version/platform matrices and strict task-egress scope remain unqualified |
| QUIC / HTTP/3 decode | unsupported | External target-process UDP is counted as `possible_quic_connections` and fails transport verification; it is not silently ignored or assumed to be model traffic |
| macOS / Linux arm64 / Recorder-or-Agent inside a container | unknown | No release-gate matrix yet. This does not refer to the platform's own API/Worker/Web Compose containers, whose exact Linux x86-64 images are qualified separately. |
| 5-hour steady-state soak | verified | The operator-approved 18,000-second core gate passed against exact release SHA-256 `9844f977fc3f4b4f466dac8fb1ec8495d835da38e6aaa4aaeb083fb57e873869`: 18,000/18,000 requests, 522,023 events, and 216,000 blobs with zero client error, capture drop, incomplete attempt, event tail, missing blob, or corrupt blob. The atomic report and independent post-run blob/integrity checks passed. |
| 20-collector connected soak | verified | Twenty persistent collectors each exceeded 18,000 measured seconds and completed 6,000/6,000 attempts, 174,346 local events, and 1,137 server Recording segments. The 900-second API outage, collector and Worker restarts, stable identities, local/spool/server sequence and integrity checks, final drain, exact runtime provenance, and remote-plus-local deletion propagation all passed. |

No matrix entry changes the current manifest claim from `best-effort`.
